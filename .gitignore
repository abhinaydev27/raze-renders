# macOS junk
.DS_Store

# local-only tooling — not part of the deployed site
.claude/
zip-site.command
publish.command
add-work.command
tools/

# source plates the reel is rendered FROM — the finished mp4 ships, these don't
assets/plates/

# raw exports waiting to be transcoded — only the converted mp4 ships
incoming/
