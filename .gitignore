# macOS junk
.DS_Store

# local-only tooling — not part of the deployed site
.claude/
zip-site.command
publish.command
tools/

# source plates the reel is rendered FROM — the finished mp4 ships, these don't
assets/plates/
